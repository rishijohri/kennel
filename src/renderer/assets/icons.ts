// Brand icons (downscaled from the 2048px originals in /assets for crisp,
// lightweight in-app use). The full-resolution source art ships as the
// packaged app icon via electron-builder (build/icon.png).
import appIcon from './app.png'
import caretakerIcon from './caretaker.png'
import walkerIcon from './walker.png'

export { appIcon, caretakerIcon, walkerIcon }
